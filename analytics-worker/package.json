{
  "name": "ghosts-in-shells-analytics",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy:staging": "wrangler deploy --env staging",
    "deploy:production": "wrangler deploy --env production",
    "db:local": "wrangler d1 migrations apply DB --env staging --local",
    "db:staging": "wrangler d1 migrations apply ghosts-in-shells-analytics-staging --env staging --remote",
    "db:production": "wrangler d1 migrations apply ghosts-in-shells-analytics-production --env production --remote",
    "test": "node --test test/*.test.js",
    "create:test-invite": "node scripts/create-test-invite.js"
  },
  "devDependencies": {
    "wrangler": "^4.36.0"
  }
}
